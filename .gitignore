# Dependency directories
# These folders contain thousands of files and should never be committed.
# They are installed automatically by running `npm install`.
/node_modules
/client/node_modules
/server/node_modules

# Build output
# This is the compiled, production-ready version of your React app.
# It's generated by running `npm run build` in the client folder.
/client/build

# Environment variables
# IMPORTANT: These files contain your secret keys and passwords.
# They should NEVER be committed to GitHub for security reasons.
.env
client/.env
server/.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Logs
# Log files can be large and are specific to your machine.
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Editor directories and files
# These are specific to your code editor (like VS Code).
.vscode
.idea
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Operating System generated files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db